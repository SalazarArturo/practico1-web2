<%- include('../../components/home/header.ejs') %>
   

<div class="container mt-4">

    <h2>Gestion de Canchas</h2>

    <!-- si hay error -->
    <% if(error){ %>
        <p class="text-danger mt-2"><%= error %></p>
    <% } %>

    <!-- no hay canchas -->
    <% if(!error && canchas.length === 0){ %>
        <p class="text-muted mt-2">No hay canchas registradas todavia</p>
    <% } %>

    <!-- esto esta sin importar el caso -->
    <a href="/user/admin/canchas/new" class="btn btn-primary mb-3">
        Crear nueva cancha
    </a>

    <!-- hay canchas-->
    <% if(!error && canchas.length > 0){ %>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Precio</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>

            <tbody>
                <% canchas.forEach(cancha => { %>
                    <tr>
                        <td><%= cancha.nombre %></td>
                        <td><%= cancha.precio_por_hora %></td>
                        <td>
                            <% if(cancha.estado === 'activa'){ %>
                                <span class="badge bg-success">Activa</span>
                            <% } else { %>
                                <span class="badge bg-secondary">Inactiva</span>
                            <% } %>
                        </td>
                        <td>
                            <a href="/user/admin/canchas/<%= cancha.id %>/edit" 
                               class="btn btn-warning btn-sm">
                                Editar
                            </a>
                            <form action="/user/admin/canchas/<%= cancha.id %>/delete" 
                                  method="post" 
                                  style="display:inline;">
                                <button class="btn btn-danger btn-sm">
                                    Eliminar
                                </button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } %>
</div>
<%- include('../../components/home/footer.ejs') %>